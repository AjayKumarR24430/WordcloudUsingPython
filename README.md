# WordcloudUsingPython
This repository consists of how to build a word-cloud from any text document using Python.

The python code describes the steps to create a word cloud. Import the libraries numpy, wordcloud for generating frequencies of words and 
matplotlib for plotting the wordcloud. 
